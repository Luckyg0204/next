import Image from "next/image";

export default function Home() {
  await default async function Home{
    const student = undefined;
    await new Promise(resolve=>setTimeout(resolve,6000);
  return(
    
  ) 
)
  }
}
